import { Box } from 'reakit'
import styled from 'styled-components'
import { CardStyles } from '../CharacterDropdown'

export const PlayerCard = ({ children }) => {
    return <CardStyles>{children}</CardStyles>
}
